Controller   <-----> Model
 |
 |
 v
 View

 MVC (design pattern GUI)

 // Model View Controller
/*class User {
	constructor(id, name, pass) {
		this.id = id;
		this.email = name;
		this.hash = hash(pass);

		this.name = null;
		this.age = 0;
	}

	checkPassword(password) {
		return hash(password) == this.hash;
	}

	validate() {
		return validateUsername() && validateEmail();
	}

	validateUsername(){ return true; }
	validateEmail() {return true;}
}

class UserDB {
	constructor() {
		this.db = [];
	}

	addUser(user) {
		this.db.push_back()
	}
}

() => {}
*/